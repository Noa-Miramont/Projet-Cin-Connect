{
  "name": "dollyzoom",
  "version": "1.0.0",
  "private": true,
  "description": "Mono-repo Dolly Zoom - Full stack JavaScript",
  "scripts": {
    "predev": "pnpm --filter @dollyzoom/shared run build",
    "dev": "concurrently -n fe,be \"pnpm --filter @dollyzoom/frontend run dev\" \"pnpm --filter @dollyzoom/backend run dev\"",
    "dev:frontend": "pnpm --filter @dollyzoom/frontend run dev",
    "dev:backend": "pnpm --filter @dollyzoom/backend run dev",
    "build": "pnpm --filter @dollyzoom/shared run build && pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "seed": "pnpm --filter @dollyzoom/backend run seed"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.30.3",
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.21.5"
    }
  }
}
